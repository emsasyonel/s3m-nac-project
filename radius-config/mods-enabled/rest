rest {
    connect_uri = "http://nac_fastapi:8000"

    authorize {
        uri = "${..connect_uri}/authorize"
        method = 'post'
        body = 'json'
    }

    authenticate {
        uri = "${..connect_uri}/auth"
        method = 'post'
        body = 'json'
    }

    accounting {
        uri = "${..connect_uri}/accounting"
        method = 'post'
        body = 'json'
    }
}
